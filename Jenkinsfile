pipeline {
    agent {
        label 'main'
    }
    stages {
        stage('Sincronización') {
            steps {
                echo '***** Se inicio la sincronización de los repositorios *****'
            }
        }
    }
}
